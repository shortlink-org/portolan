{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/shortlink-org/portolan/schema/portolan.schema.json",
  "title": "portolan manifest",
  "description": "Where the facts come from, what produces them, and what turns them into something to read. Generated by scripts/schema.mjs from the plugins this manifest declares; run `npm run schema` after changing them.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sources"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "$comment": {
      "type": "string"
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "Globs of the catalog fragments that make up the estate. They are merged, then validated as a union, so a fragment naming a peer it does not own is normal rather than broken."
    },
    "plugins": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/plugin"
      },
      "description": "How to run each plugin. A step names one by the name declared here."
    },
    "extract": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "plugin",
          "in",
          "out"
        ],
        "properties": {
          "plugin": {
            "enum": [
              "go-domain",
              "openapi",
              "sql",
              "flows"
            ],
            "description": "A plugin declared above, one that belongs in this phase."
          },
          "in": {
            "type": "string",
            "description": "The directory to read, relative to the repository."
          },
          "out": {
            "type": "string",
            "description": "Where what comes back is written. A plugin names files; nothing it names is allowed to land outside this directory."
          },
          "options": {
            "type": "object",
            "description": "Passed to the plugin unread. What it accepts is described below."
          }
        },
        "allOf": [
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "go-domain"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.go-domain"
                }
              }
            }
          },
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "openapi"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.openapi"
                }
              }
            }
          },
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "sql"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.sql"
                }
              }
            }
          },
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "flows"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.flows"
                }
              }
            }
          }
        ]
      },
      "description": "Steps that read source and write catalog fragments. They run first, before there is a catalog."
    },
    "verify": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "plugin",
          "in",
          "out"
        ],
        "properties": {
          "plugin": {
            "enum": [
              "otel"
            ],
            "description": "A plugin declared above, one that belongs in this phase."
          },
          "in": {
            "type": "string",
            "description": "The directory to read, relative to the repository."
          },
          "out": {
            "type": "string",
            "description": "Where what comes back is written. A plugin names files; nothing it names is allowed to land outside this directory."
          },
          "options": {
            "type": "object",
            "description": "Passed to the plugin unread. What it accepts is described below."
          }
        },
        "allOf": [
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "otel"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.otel"
                }
              }
            }
          }
        ]
      },
      "description": "Steps that read something observed - traces, a test's record - and answer with a fragment about the catalog somebody else declared."
    },
    "generate": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "plugin",
          "out"
        ],
        "properties": {
          "plugin": {
            "enum": [
              "markdown"
            ],
            "description": "A plugin declared above, one that belongs in this phase."
          },
          "out": {
            "type": "string",
            "description": "Where what comes back is written. A plugin names files; nothing it names is allowed to land outside this directory."
          },
          "options": {
            "type": "object",
            "description": "Passed to the plugin unread. What it accepts is described below."
          }
        },
        "allOf": [
          {
            "if": {
              "required": [
                "plugin"
              ],
              "properties": {
                "plugin": {
                  "const": "markdown"
                }
              }
            },
            "then": {
              "properties": {
                "options": {
                  "$ref": "#/$defs/options.markdown"
                }
              }
            }
          }
        ]
      },
      "description": "Steps that read the merged catalog and write something to read."
    }
  },
  "$defs": {
    "plugin": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "oneOf": [
        {
          "required": [
            "wasm"
          ]
        },
        {
          "required": [
            "process"
          ]
        }
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "What the steps below call this plugin. It need not be the plugin's own name."
        },
        "wasm": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "url"
          ],
          "description": "The default. The module gets no filesystem, no network and no environment, so a plugin from somebody else's repository can be run over your source tree without reading it.",
          "properties": {
            "url": {
              "type": "string",
              "pattern": "^(https://|file://)",
              "description": "https:// to download, or file:// for a path beside the manifest, as in file://plugins/gen-markdown.wasm."
            },
            "sha256": {
              "type": "string",
              "pattern": "^[0-9a-f]{64}$",
              "description": "Checksum of the module. Required for a download; optional for a local file, which it does not protect."
            }
          }
        },
        "process": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "cmd"
          ],
          "description": "The escape hatch for a plugin that needs a toolchain. Same protocol, none of the sandbox, which is the trade being made.",
          "properties": {
            "cmd": {
              "type": "string",
              "description": "The command to run, as in `go run ./plugins/extract-go`."
            }
          }
        }
      }
    },
    "options.go-domain": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "context": {
          "type": "string",
          "description": "Slug of the bounded context this service belongs to. A Go module knows its import path, not which context owns it. Defaults to the name of the input directory.",
          "examples": [
            "auth",
            "shop"
          ]
        },
        "contextName": {
          "type": "string",
          "description": "What the context is called on the page. Defaults to the slug, titled.",
          "examples": [
            "Authentication"
          ]
        },
        "contextSummary": {
          "type": "string",
          "description": "A sentence or two on what the context is responsible for. Read on the context page and nowhere else."
        },
        "classification": {
          "enum": [
            "core",
            "supporting",
            "generic"
          ],
          "description": "Whether the context is core to the business, supports it, or is generic and could be bought."
        },
        "service": {
          "type": "string",
          "description": "Slug of the service. Defaults to the name of the input directory.",
          "examples": [
            "auth"
          ]
        },
        "serviceName": {
          "type": "string",
          "description": "What the service is called on the page. Defaults to the title of its README, and failing that to the slug, titled."
        },
        "repo": {
          "type": "string",
          "description": "Where the source lives. Defaults to the module path in go.mod.",
          "examples": [
            "github.com/shortlink-org/portolan"
          ]
        },
        "store": {
          "type": "string",
          "description": "Slug of the database this service keeps its state in, the same one the sql extractor is given. It is here for the flows: a call on a repository lands somewhere, and only the manifest knows where.",
          "examples": [
            "pg"
          ]
        },
        "peers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Which service answers for what this one calls, as the callee's package to its service id: a proto package for a gRPC client, the api id (the document's title and major version, `auth.v1`) for a generated HTTP client. The generated client names the package and the operation; only the manifest knows whose it is. A package with no line here is called as unknown and its steps stay unresolved.",
          "examples": [
            {
              "risk.v1": "shop.risk",
              "auth.v1": "auth.auth"
            }
          ]
        },
        "out": {
          "type": "string",
          "description": "Name of the fragment file, inside the step's output directory. One extractor writes one file, so that a fragment carries the provenance of the run that produced it.",
          "default": "domain.json"
        }
      }
    },
    "options.openapi": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "context": {
          "type": "string",
          "description": "Slug of the bounded context that owns the service this document belongs to. Defaults to the name of the input directory.",
          "examples": [
            "auth"
          ]
        },
        "service": {
          "type": "string",
          "description": "Slug of the service the document describes. Defaults to the name of the input directory.",
          "examples": [
            "auth"
          ]
        },
        "spec": {
          "type": "string",
          "description": "The OpenAPI document, relative to the input root. Left out, the extractor looks for one.",
          "examples": [
            "internal/infrastructure/transport/http/gen/openapi.yaml"
          ]
        },
        "api": {
          "type": "string",
          "description": "Prefix for the ids of the rpc services this produces. Left out, it is built from the document's title and major version: auth 1.0.0 gives auth.v1, and a tag called users gives auth.v1.Users.",
          "examples": [
            "auth.v1"
          ]
        },
        "out": {
          "type": "string",
          "description": "Name of the fragment file, inside the step's output directory.",
          "default": "api.json"
        }
      }
    },
    "options.sql": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "context": {
          "type": "string",
          "description": "Slug of the bounded context that owns the service these migrations belong to. Defaults to the name of the input directory.",
          "examples": [
            "auth"
          ]
        },
        "service": {
          "type": "string",
          "description": "Slug of the service that owns the database. A store belongs to exactly one service. Defaults to the name of the input directory.",
          "examples": [
            "auth"
          ]
        },
        "store": {
          "type": "string",
          "description": "Slug of the database these migrations build. One service can keep state in more than one, and a slug is how the two are told apart. Give the go-domain extractor the same one.",
          "default": "pg"
        },
        "name": {
          "type": "string",
          "description": "What the database is called on the page.",
          "examples": [
            "Auth database"
          ]
        },
        "kind": {
          "enum": [
            "postgres",
            "mysql",
            "sqlite",
            "redis",
            "mongodb",
            "clickhouse",
            "s3",
            "kafka-topic",
            "other"
          ],
          "description": "What kind of store it is.",
          "default": "postgres"
        },
        "repositories": {
          "type": "string",
          "description": "Where the adapters live, relative to the input root. The migrations are read from there, and so is which aggregate a table persists.",
          "default": "internal/infrastructure/repository"
        },
        "out": {
          "type": "string",
          "description": "Name of the fragment file, inside the step's output directory.",
          "default": "stores.json"
        }
      }
    },
    "options.markdown": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "description": "Heading of the index page. The catalog does not name the estate, so this is the one thing the generator cannot derive.",
          "examples": [
            "Example estate"
          ]
        }
      }
    },
    "options.otel": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "traces"
      ],
      "properties": {
        "traces": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "The recordings to read, as globs relative to the input root. OTLP JSON, one batch per file or one per line.",
          "examples": [
            [
              "telemetry/traces.jsonl"
            ]
          ]
        },
        "services": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Which catalog service a resource's service.name belongs to, for an estate where the two differ. Without a line here a name is matched to the one service whose slug it is.",
          "examples": [
            {
              "auth-api": "auth.auth"
            }
          ]
        },
        "events": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Which catalog event a wire name belongs to, for an estate where the last segment of the event.name attribute is not the event's name in the model.",
          "examples": [
            {
              "auth.PasswordChanged": "auth.auth.PasswordChanged"
            }
          ]
        },
        "out": {
          "type": "string",
          "description": "Name of the fragment file, inside the step's output directory."
        }
      }
    },
    "options.flows": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The flow files to read, as globs relative to the input root. Left out, every *.flow.md directly under the root.",
          "examples": [
            [
              "*.flow.md"
            ]
          ]
        },
        "out": {
          "type": "string",
          "description": "Name of the fragment file, inside the step's output directory. Left out, flows.json."
        }
      }
    }
  }
}
