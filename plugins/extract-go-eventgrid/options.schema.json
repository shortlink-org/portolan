{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "context": {
      "type": "string",
      "description": "Slug of the bounded context this service belongs to. Defaults to the name of the input directory.",
      "examples": ["shop"]
    },
    "service": {
      "type": "string",
      "description": "Slug of the service. Defaults to the name of the input directory.",
      "examples": ["fulfillment"]
    },
    "domains": {
      "type": "array",
      "description": "Event Grid domain names or endpoint URLs. Event Grid uses the same endpoint shape for custom topics and domains, so listing domains lets CloudEvent source values be read as domain topic names.",
      "items": {
        "type": "string"
      },
      "examples": [["estate", "https://notifications.westus2-1.eventgrid.azure.net/api/events"]]
    },
    "out": {
      "type": "string",
      "description": "Name of the fragment file, inside the step's output directory.",
      "default": "eventgrid.json"
    }
  }
}
