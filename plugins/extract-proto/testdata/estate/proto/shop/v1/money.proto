syntax = "proto3";

package shop.v1;

import "buf/validate/validate.proto";

// An amount and what it is denominated in.
message Money {
  string currency = 1 [(buf.validate.field).string = { len: 3, in: ["USD", "EUR"] }];
  int64 amount_minor = 2 [(buf.validate.field).int64.gte = 0];
}
